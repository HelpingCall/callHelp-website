{% extends 'emails/mail_base.html.twig' %}
{% block body %}
    {% embed 'emails/blocks/1-col.twig' %}
        {% block title %}{{ salutation }} {{ title }} {{ lastname }} {{ 'mail.acount-confirm.activate-account'|trans }}{% endblock %}
        {% block body %}
<p>{{ 'mail.acount-confirm.activate-account.text'|trans }}</p>
{% include 'emails/blocks/button.twig' with {'title':"Email verifizieren", 'href': confirmLink } %}
    <p>
    </p>
    <p>
    </p>
    <p>
        {{ 'mail.acount-confirm.activate-account.no-button'|trans }}
        <a href="{{ confirmLink }}">{{ confirmLink }}</a>
    </p>
        {% endblock %}
    {% endembed %}
{% endblock %}
